header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /index.html

  - titles:
      # @start locale config
      en      : &EN       Members
      # @end locale config
    url: /members.html

  - titles:
      # @start locale config
      en      : &EN       Publications
      # @end locale config
    url: /publications.html

  - titles:
      # @start locale config
      en      : &EN       Courses
      # @end locale config
    url: /courses.html

