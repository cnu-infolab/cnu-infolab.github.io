header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /index.html

  - titles:
      # @start locale config
      en      : &EN       People
      # @end locale config
    url: /people.html

  - titles:
      # @start locale config
      en      : &EN       Publications
      # @end locale config
    url: /publications.html

  - titles:
      # @start locale config
      en      : &EN       Courses
      # @end locale config
    url: /courses.html

